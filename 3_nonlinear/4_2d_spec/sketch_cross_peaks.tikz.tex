%\documentclass[margin=0mm]{standalone}
%\usepackage{tikz}
%\usepackage{pgfplots}
% \pgfplotsset{compat=newest}
%
%
%\usepgfplotslibrary{groupplots}
%
%\usepackage{currfile,hyperxmp,calc}
%\usetikzlibrary{calc,math}
%\usetikzlibrary{calc,patterns,angles,quotes}
%
%
%\usetikzlibrary{angles,quotes}
%
%\usetikzlibrary{math,matrix,fit,positioning,intersections}
%\usetikzlibrary{patterns,decorations.pathmorphing,decorations.markings}
%\usetikzlibrary{calc}
%\usetikzlibrary{arrows.meta} %needed tikz library
%\usetikzlibrary{quotes,angles}
%
%\usepackage{braket}
%
% 
%
%
%
%
%\begin{document}


\begin{tikzpicture}%[font=\footnotesize]

%\draw (-1,-3) rectangle ++(11,7);

%\useasboundingbox (0.,0) rectangle (10.6,8.3);
\tikzmath{ \L = 0.7; \H=1.2; \dx = 0.7; \w1 = 0.8; \w2=1.5; \sy = 2.2;}

%------- 2x2 levels
\begin{scope}
	\draw[thick](-\dx,0) -- ++ (\L,0);
	\draw[thick] (-\dx,\H * \w1) -- ++ (\L,0);
	\draw[->](-\dx + \L / 2,0) -- ++ (0,\H * \w1);

	\draw[thick](\dx,0) -- ++ (\L,0);
	\draw[thick](\dx,\H * \w2) -- ++ (\L,0);
	\draw[->](\dx+ \L / 2,0) -- ++ (0,\H * \w2);
\end{scope}

%----- V system
\begin{scope}[yshift=-30mm]
	\draw[thick](0,0) -- ++ (\L,0);
	\draw[thick] (-\dx,\H * \w1) -- ++ (\L,0);
	\draw[thick](\dx,\H * \w2) -- ++ (\L,0);

	\draw[->](\L / 4,0) -- (-\dx + \L / 2,\H * \w1);
	\draw[->](\L * 3 / 4,0) -- (\dx+ \L / 2,\H * \w2);
\end{scope}


\foreach \u in {0,1}
{
\begin{scope}[xshift=30mm, yshift = \u*-30mm]
\draw[thick,->] (0,0) -- node[below] {$\omega_{probe}$} (\sy,0);
\draw[thick,->] (0,0) --  node [above,rotate=90] {$\sigma_{abs}$} (0,\sy);

\draw[line width=7pt, gray] (\w1,0) -- ++ (0,0.9*\sy);
\draw[line width=7pt, gray] (\w2,0) -- ++ (0,0.6*\sy);
\end{scope}
}


\foreach \u in {0,1}
{
\begin{scope}[xshift=70mm, yshift = \u*-30mm]
\draw[thick,->] (0,0) -- node[below] {$\omega_{probe}$} (\sy,0);
\draw[thick,->] (0,0) --  node [above,rotate=90] {$\omega_{pump}$} (0,\sy);

\draw[dashed] (0,0) -- (\sy, \sy);
\draw[fill = gray] (\w1, \w1) circle (3.5pt);
\draw[fill = gray] (\w2, \w2) circle (3.5pt);
\end{scope}
}

\begin{scope}[xshift=70mm, yshift=-30mm]
\draw[fill = blue] (\w1, \w2) circle (3.5pt);
\draw[fill = blue] (\w2, \w1) circle (3.5pt);
\end{scope}





  
\end{tikzpicture}



%\end{document}